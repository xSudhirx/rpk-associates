{
  "name": "rpk-fullstack",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start:prod": "npm install --prefix server && node server/src/index.js"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}